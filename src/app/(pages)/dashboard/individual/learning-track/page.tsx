import React from 'react'

const page = () => {
  return (
    <div>
        <button>
            Create Customized learning
        </button>
        <button>
            <select defaultChecked> Slect from one of the below</select>
            <select name="" id="">Beggineer</select>
            <select > Intermidiate</select>
            <select>Expert</select>
        </button>
    </div>
  )
}

export default page